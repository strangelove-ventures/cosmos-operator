apiVersion: v1
kind: Service
metadata:
  name: {{ include "cosmos-operator.fullname" . }}
  labels:
    {{- include "cosmos-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cosmos-operator.selectorLabels" . | nindent 4 }}
