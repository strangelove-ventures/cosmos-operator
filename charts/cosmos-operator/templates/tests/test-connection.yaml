apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmos-operator.fullname" . }}-test-connection"
  labels:
    {{- include "cosmos-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosmos-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
